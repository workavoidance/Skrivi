# Scope: this TTS handoff and source snapshot

Read START_HERE.md and CONTINUATION.md before making changes. They supplement the earlier ../HANDOFF.md and take precedence for later model and implementation decisions.

Reuse existing work. This contains the separate VoxCPM2 Reader source, not the original multi-model shootout. Locate that shootout before extending it. If a focused search cannot find it, ask for its location; do not rebuild it silently.

Do not modify Skrivi runtime, website, installer, model storage or settings as part of TTS work. This branch is a handoff carrier. Keep uncommitted user work safe.

Read the existing code and reports before running archived build scripts. Model weights are not Git assets. Reuse LocalAppData caches. Preserve native cadence and audio endings. Record actual hardware, effective CPU/GPU backend and tested versus untested claims.

Update a project-status/inventory document with source paths, last working version, dependencies, build commands, evidence and next steps after substantive work.
